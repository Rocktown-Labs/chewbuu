# Use same-origin URLs for local web development and Portless.
VITE_SERVER_URL=/
VITE_BLOCKS_API_URL=/aws-blocks/api

# Optional override for the Vite development proxy target.
# BLOCKS_DEV_API_URL=http://127.0.0.1:3000
